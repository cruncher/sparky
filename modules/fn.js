
import prevent  from './fn.prevent.js';
import _if      from './fn.if.js';
import starton  from './fn.start-on.js';

export default {
    'if':        _if,
    'prevent':   prevent,
    'start-on':  starton,
};
